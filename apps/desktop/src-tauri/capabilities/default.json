{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal Phase 0 grant for the main window: core defaults plus the two shell commands.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-get-app-info", "allow-frontend-ready"]
}
