// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ApprovalStatus } from "./ApprovalStatus";
import type { Capability } from "./Capability";
import type { Risk } from "./Risk";
import type { SensitiveKind } from "./SensitiveKind";

/**
 * An approval card: what a worker wants to do, why it needs the owner, and the outcome.
 */
export type ApprovalView = { id: string, taskId: string, status: ApprovalStatus, requestedAt: number, expiresAt: number | null, resolvedAt: number | null, resolvedBy?: string, 
/**
 * The worker's title ("Backend Developer").
 */
worker: string, role: string, project?: string, folder?: string, capability?: Capability, 
/**
 * "Run programs".
 */
capabilityLabel: string, 
/**
 * What it wants to do, in a line ("Run npm publish").
 */
summary: string, 
/**
 * The exact action: a command line, a path, a script.
 */
detail: string, 
/**
 * Why it needs the owner.
 */
reason: string, risk?: Risk, riskLabel: string, sensitive?: SensitiveKind, sensitiveLabel?: string, sessionId?: string, grantId?: string, 
/**
 * A worker is waiting for the answer right now.
 */
waiting: boolean, 
/**
 * The note recorded with the outcome.
 */
note?: string, };
